name: german-credit-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_dir: {type: path}
      out_dir: {type: string}
    command: "python modelling.py --data-dir {data_dir} --out-dir {out_dir}"
